# Compass: Stage environment (CloudFront .cloudfront.net URL)
# This file is committed to git. It contains NO secrets.
# Secrets (PostHog key, Groq key, Formspree ID) go in .env.local.

NEXT_PUBLIC_APP_ENV=stage
