# Compass: Multi-Environment Deployment Configuration
#
# This file maps environment names to AWS resources. It is read by
# scripts/deploy.sh and should be committed to git (no secrets).
#
# Format: ENVNAME_VARIABLE=value
# The deploy script reads the block matching the --env flag.

# == Stage (current CloudFront deployment) =====================================
STAGE_S3_BUCKET=compass-immigration-insights-883107059193
STAGE_CF_DIST=E1LPLTVZ0035Q5
STAGE_REGION=us-east-1
STAGE_URL=https://d10immmzyp7xgr.cloudfront.net

# == Production (future custom domain) ========================================
# Uncomment and fill when prod infrastructure is provisioned via Terraform.
# PROD_S3_BUCKET=compass-prod-883107059193
# PROD_CF_DIST=EXXXXXXXXXX
# PROD_REGION=us-east-1
# PROD_URL=https://your-domain.com
